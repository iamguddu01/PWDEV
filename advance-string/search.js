// Only gives index
// If the result is not available the output will be -1 instead of null
let text = "I love javascript, Javascript 100 times more powerfull than python, Hello@world#2025";

// let result = text.search("javascript");

// let result = text.search(/Javascript/i); // i flag to ignore cases

// let result = text.search(/\d/); // \d will return the index of numeric integer occurence

// let result = text.search(/[@#!]/);  // Return the index of special character, It is user specified set of special charcters and can be modified

// let result = text.search(/[A-Z]/); // Return the index of first occurence of capital letter


console.log(result);